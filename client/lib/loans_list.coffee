class @LoansList
  constructor : ->




# template = (d) ->
#   "<h3>#{d.title}</h3>"

# @createList = ->

# @renderList = ->
#   data = Loans.find().fetch()

#   ul = d3.select('ul.loans')
#          .selectAll('li')
#          .data(data)
#          .classed('current', (d) -> )
#          .html(template)
